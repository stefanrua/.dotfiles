#!/bin/bash

dotfiles=(".bashrc .xinitrc .Xresources")
configs=("nvim i3 i3status")

for i in ${dotfiles[@]}; do
    echo "Symlinking $i..."
    ln -s $PWD/$i $HOME/
done

for i in ${configs[@]}; do
    echo "Symlinking $i..."
    ln -s $PWD/$i $HOME/.config/
done

echo "Done!"
